name: Test React Application
on:
  pull_request:
    branches: [ development ]
  workflow_dispatch:
defaults:
  run:
    working-directory: ./
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Run ESLint
        run: npm run lint